<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BookStore.ViewModels.Author"
             xmlns:model="clr-namespace:BookStoreApi"
             x:Class="BookStore.Views.Author.DetailsAuthorPage"
             Shell.PresentationMode="ModalAnimated"
             Title="Author">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Button Text="Add Books" Command="{Binding AddItemCommand}" />
            <Label Text="Name" FontSize="Medium" />
            <Entry Text="{Binding Name}" FontSize="Medium" />

            <Label Text="Surname" FontSize="Medium" />
            <Entry Text="{Binding Surname}" FontSize="Medium" />

            <Label Text="Nickname" FontSize="Medium" />
            <Entry Text="{Binding Nickname}" FontSize="Medium" />

            <Label Text="Books" FontSize="Medium" />
            
            <RefreshView x:DataType="local:DetailsAuthorViewModel" Command="{Binding LoadItemsCommand}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
                <CollectionView x:Name="ItemsListView"
                ItemsSource="{Binding Items}"
                SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout Padding="10" x:DataType="model:BookForView">
                                <Label Text="{Binding Id}" 
                            LineBreakMode="NoWrap" 
                            Style="{DynamicResource ListItemTextStyle}" 
                            FontSize="12" />
                                <Label Text="{Binding Title}" 
                            LineBreakMode="NoWrap" 
                            Style="{DynamicResource ListItemTextStyle}" 
                            FontSize="16" />
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>

            <Label Text="Creation Date" FontSize="Medium" />
            <Label Text="{Binding CretionDate}" FontSize="Medium" />

            <Label Text="Modification Date" FontSize="Medium" />
            <Label Text="{Binding MmodifDate}" FontSize="Medium" />
            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Delete" Command="{Binding DeleteCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>